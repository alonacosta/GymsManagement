@model GymManagement.Data.Entities.Position

@{
    ViewData["Title"] = "Details";
}

<div class="shadow-none p-3 mb-5 bg-body-tertiary rounded bg-white full-height-appoint">
    <h3>Details</h3>
    <div>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Name)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning"><i class="fas fa-save"></i> Save</a>
        <a asp-action="Index" class="btn btn-success"><i class="fas fa-chevron-left"></i> Back to List</a>
    </div>
</div>